# Cloud — AWS, Azure, GCP

This folder contains cloud provider topics. Create topic files inside provider subfolders (e.g. `aws/01-ec2-basics.md`).

Use the repository `TEMPLATE.md` for formatting Q/A entries.
